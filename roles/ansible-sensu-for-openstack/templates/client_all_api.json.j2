{
   "client": {
      "name": "{{ sensu_client_hostname }}",
      "address": "{{ sensu_client_address }}",
      "subscriptions": [ "{{ "\",\"".join(sensu_client_subscription_names) }}" ],
      "API": {
         "over_os_username": "{{ over_os_username }}",
         "over_os_password": "{{ over_os_password }}",
         "over_os_tenant_name": "{{ over_os_tenant_name }}",
         "over_os_auth_url": "{{ over_os_auth_url }}",
         "under_os_username": "{{ under_os_username }}",
         "under_os_password": "{{ under_os_password }}",
         "under_os_tenant_name": "{{ under_os_tenant_name }}",
         "under_os_auth_url": "{{ under_os_auth_url }}"
      }
   }
}
